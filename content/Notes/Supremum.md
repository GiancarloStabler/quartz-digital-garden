#definition #analysis/real-analysis 

#### Definition
We say that $M \in \mathbb{R}$ is the supremum of a set $S \subset \mathbb{R}$ if the following are true,
1) $M$ is an [[Maximum#Upper Bound|upper bound]] of $S$ (therefore $S$ is bounded above).
2) For any upper bound $M'$ of $S$, $M \leq M'$ ($M$ is unique).

$M$ is also called the least upper bound.

We denote the supremum of a set $S$, $\sup(S)$.

#### Examples


